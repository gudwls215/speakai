import 'package:flutter/material.dart';

class FreeTalkTab extends StatelessWidget {
  const FreeTalkTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Free Talk Page'),
    );
  }
}